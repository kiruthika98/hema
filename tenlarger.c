#include <stdio.h>
  void main() {
    int s[10];
    int i;
    int greater;
    printf("Enter ten values:");
    for (i = 0; i < 10; i++) {
    	scanf("%d", &a[i]);
    }
    greater = s[0];
    for (i = 0; i < 10; i++)
    {
   	if (s[i] > greatest)
  {
	greater = s[i];
    }
    }
    printf("Greatest of ten numbers is %d", greater);
  }
